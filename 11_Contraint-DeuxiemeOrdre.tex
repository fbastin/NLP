\documentclass[usepdftitle=false,aspectratio=169]{beamer}

\usepackage[utf8]{inputenc}
\usetheme{Singapore}
\usepackage{xcolor}
\setbeamertemplate{footline}[frame number]

% \setbeamercovered{transparent}
%\usecolortheme{crane}
\title[IFT3515]{IFT 3515\\Fonctions à plusieurs variables\\Optimisation avec contraintes\\Conditions d'optimalité -- $2^e$ ordre}
\author[Fabian Bastin]{Fabian Bastin\\DIRO\\Université de Montréal}
\date{}

\usepackage{enumerate}
\usepackage[francais]{babel}

\usepackage{easybmat}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,calc,decorations.markings}
\usepackage[absolute,overlay]{textpos}
\setlength{\TPHorizModule}{1cm}
\setlength{\TPVertModule}{1cm}

\newtheorem{defn}{Définition}
\newtheorem{lem}{Lemme}
\newtheorem{thm}{Théorème}
\newtheorem{coro}{Corollaire}

\def\red{\color{red}}
\def\blue{\color{blue}}

\def\co{\mathcal{o}}

\def\cA{\mathcal{A}}
\def\cB{\mathcal{B}}
\def\cE{\mathcal{E}}
\def\cI{\mathcal{I}}
\def\cL{\mathcal{L}}
\def\cN{\mathcal{N}}
\def\cR{\mathcal{R}}

\def\bu{\boldsymbol{u}}

\def\cX{\mathcal{X}}

\def\NN{\mathbb{N}}
\def\RR{\mathbb{R}}

\setbeamertemplate{footline}[frame number]

\begin{document}
\frame{\titlepage}

% ------------------------------------------------------------------------------------------------------------------------------------------------------

\begin{frame}
\frametitle{Contexte}

%Notes partiellement basées sur \url{http://www.numerical.rl.ac.uk/people/nimg/course/lectures/raphael/lectures/lec10slides.pdf}

%\mbox{}

Nous considérons à nouveau le problème d'optimisation nonlinéaire (PNL) général
\begin{align*}
\min_{x \in \RR^n}\ & f(x) \\
\mbox{t.q. } & g_i(x) = 0,\ i\ \in \cE \\
& g_i(x) \leq 0,\ i \in \cI
\end{align*}

Hypothèse: $f$ et $g_i$ ($i \in \cE \cup \cI$) sont des fonctions deux fois continûment différentiables.

\end{frame}

\begin{frame}
\frametitle{Chemin de sortie réalisable: rappel}

\begin{columns}[T]
\begin{column}{0.52\textwidth}
\begin{defn}
Soit $x^* \in \RR^n$ réalisable et $x \in C^2((-\epsilon,\epsilon), \RR^n)$ un chemin tel que
\begin{align*}
x(0) &= x^* \\
d &:= \frac{d}{dt}x(0) \ne 0 \\
g_i(x(t)) &= 0,\ i \in \cE,\ t \in (-\epsilon, \epsilon) \\
g_i(x(t)) &\leq 0,\ i \in \cI,\ t \in [0, \epsilon)
\end{align*}
$x(t)$ est {\red un chemin de sortie réalisable} et $d$ est {\red une direction de sortie réalisable} à partir de $x^*$.
\end{defn}
\end{column}
\begin{column}{0.45\textwidth}
\centering
\begin{tikzpicture}[scale=1.4,>=Stealth]
  % Feasible set: g(x) <= 0, shaded region above a curve
  % Constraint curve: g(x) = x2 - x1^2 + 0.5 = 0, i.e. x2 = x1^2 - 0.5
  % Feasible region: x2 >= x1^2 - 0.5 (shaded)
  \fill[blue!10] (-1.1,0.7) -- plot[domain=-1.05:1.05,samples=40] (\x,{\x*\x-0.5}) -- (1.1,0.7) -- cycle;
  % Boundary g(x)=0
  \draw[blue!70!black,thick,domain=-1.05:1.05,samples=60,smooth]
    plot (\x,{\x*\x-0.5}) node[right,font=\small] {$g\!=\!0$};
  % x* at (0.5, -0.25)
  \coordinate (xstar) at (0.5,-0.25);
  \fill[red] (xstar) circle (1.8pt);
  \node[below right,font=\small,red] at (xstar) {$x^*$};
  % Path x(t): follow the boundary curve x1=0.5+t, x2=(0.5+t)^2-0.5 for small t
  % Parametrize along the constraint curve
  \draw[red!80!black,very thick,domain=0:0.65,samples=30,smooth,->]
    plot ({0.5+\x},{(0.5+\x)*(0.5+\x)-0.5})
    node[right,font=\small,red!80!black] {$x(t)$};
  % Direction d: tangent to curve at x* = (1, 2*0.5)=(1,1), normalized
  % tangent: dx1=1, dx2=2*x1*dx1=1 => direction (1,1)/sqrt(2)
  \draw[->,very thick,green!50!black] (xstar) -- ++(0.35,0.35)
    node[above,font=\small,green!50!black] {$d$};
  % Gradient of g at x* = (-2*x1, 1) = (-1, 1), normalized outward
  \draw[->,thick,orange!80!black] (xstar) -- ++(-0.28,0.28)
    node[left,font=\small,orange!80!black] {$\nabla g$};
  % Label feasible region
  \node[blue!60!black,font=\small] at (-0.5,0.4) {$\mathcal{X}$};
\end{tikzpicture}
\end{column}
\end{columns}

\end{frame}

\begin{frame}
\frametitle{Optimalité au second ordre}

L'analyse d'optimalité au second ordre est basée sur l'observation suivante: si $x^*$ est un minimiseur local du problème de PNL et $x(t)$ est un chemin de sortie réalisable à partir de $x^*$, alors $x^*$ doit aussi être un minimiseur local pour le problème d'optimisation sous contraintes avec une variable
$$
\min_t f(x(t)) \mbox{ t.q. } t \geq 0.
$$

\mbox{}

Analysons davantage les directions de sortie réalisables à partir de $x^*$.
La définition implique
$$
d^T \nabla g_i(x^*)
= \frac{d}{dt} g_i(x(t)) |_{t = 0}
= \begin{cases}
\frac{d}{dt} 0 = 0 & i \in \cE \\
\lim_{t \rightarrow 0^+} \frac{g_i(x(t))-0}{t} & i \in \cA(x^*) \cap \cI
\end{cases}
$$

\end{frame}

\begin{frame}
\frametitle{Conditions nécessaires}

Dès lors, les conditions suivantes sont nécessaires pour que $d \in \RR^n$ soit une direction de sortie réalisable à partir de $x^*$:
\begin{align*}
d & \ne 0 \\
d^T \nabla g_i(x^*) &= 0,\ i \in \cE \\
d^T \nabla g_i(x^*) &\leq 0,\ i \in \cA(x^*) \cap \cI
\end{align*}

\end{frame}

%\begin{frame}
%\frametitle{Conditions nécessaires}

%Si $(x^*, \lambda^*)$ satisfont les conditions KKT, le gradient du Lagrangien s'annule:
%$$
%0 = \nabla_x L(x^*,\lambda^*) = \nabla f(x^*) + \sum_{i \in \cI} \lambda_i^* \nabla g_i(x^*) + \sum_{i \in \cE} \lambda_i^* \nabla g_i(x^*)
%$$
%Soit $d \in N^+$. Alors
%\begin{align*}
%0 &= d^T\nabla_x L(x^*,\lambda^*) \\
%& = d^T\nabla f(x^*) + \sum_{i \in \cI} \lambda_i^* d^T\nabla g_i(x^*) + \sum_{i \in \cE} \lambda_i^* d^T\nabla g_i(x^*) \\
%& = d^T\nabla f(x^*) + \sum_{i \in \cA(x^*) \cap \cI} \lambda_i^* d^T\nabla g_i(x^*)
%\end{align*}

%\end{frame}

\begin{frame}
\frametitle{Conditions suffisantes}

D'autre part, si la LICQ tient en $x^*$, du Lemme sur la LICQ, nous avons l'existence d'un chemin de sortie réalisable $x(t)$ à partir de $x^*$ tel que
\begin{align*}
x(0) &= x^* \\
\frac{d}{dt} x(0) &= d \\
g_i(x(t)) &= td^T\nabla g_i(x^*),\ i \in \cE \cup \cA(x^*),\ t \in (-\epsilon, \epsilon)
\end{align*}
Dès lors, lorsque la LICQ tient, les conditions nécessaires sur $d$ sont aussi suffisantes, et fournissent par conséquent une caractérisation exacte pour que $d$ soit une direction de sortie réalisable à partir de $x^*$.

\end{frame}

\begin{frame}
\frametitle{Conditions nécessaires d'optimalité au second ordre}

Soit $x^*$ un minimum local tel que la LICQ tienne.
Les conditions KKT indiquent qu'il existe un vecteur de multiplicateurs de Lagrange $\lambda^*$ tel que
\begin{align*}
\nabla_x L(x^*,\lambda^*) &= 0 \\
g_i(x^*) &= 0,\ i \in \cE \\
g_i(x^*) &\leq 0,\ i \in \cI \\
\lambda_i^* g_i(x^*) &= 0,\ i \in \cI \\
\lambda_i^* &\geq 0,\ i \in \cI
\end{align*}
où
$$
L(x^*, \lambda^*) = f(x^*) + \sum_{i \in \cE \cup \cI} \lambda_i^* g_i(x^*).
$$

\end{frame}

\begin{frame}
\frametitle{Conditions nécessaires d'optimalité au second ordre}

Soit $x(t)$ un chemin de sortie réalisable à partir de $x^*$ avec la direction de sortie $d$, et considérons le problème restreint
\begin{align*}
\min\ & f(x(t)) \\
\mbox{t.q. } & t \geq 0.
\end{align*}
Puisque $x^*$ est un minimiseur local du PNL, $t = 0$ est aussi un minimiseur local du problème restreint.

Du développement de Taylor autour de $t = 0$ et de la règle de la chaîne,
\begin{align*}
f(x(t)) &= f(x(0)) + t\, \frac{d}{dt} f(x(t)) \bigg|_{t=0} + O(t^2) \\
&= f(x^*) + t\left(\frac{d}{dt}x(t)\bigg|_{t=0}\right)^T \nabla f(x^*) + O(t^2) \\
&= f(x^*) + td^T \nabla f(x^*) + O(t^2)
\end{align*}

\end{frame}

\begin{frame}
\frametitle{Conditions nécessaires d'optimalité au second ordre}

Les conditions KKT donnent $\nabla_x L(x^*,\lambda^*) = 0$, soit
$$
\nabla f(x^*) = -\sum_{i \in \cE \cup \cI} \lambda_i^* \nabla g_i(x^*).
$$
En substituant dans le développement de Taylor,
$$
f(x(t)) = f(x^*) - t \sum_{i \in \cE \cup \cI} \lambda_i^* d^T \nabla g_i(x^*) + O(t^2)
$$

\mbox{}

Pour que $t = 0$ soit minimiseur local du problème restreint, il faut que pour $t \geq 0$ assez petit,
$$
- t \sum_{i \in \cE \cup \cI} \lambda_i^* d^T \nabla g_i(x^*) + O(t^2) \geq 0.
$$

\end{frame}

\begin{frame}
\frametitle{Conditions nécessaires d'optimalité au second ordre}

Notons que
$$
\lambda_i^* d^T\nabla g_i(x^*) = 0,\ i \in \cE \cup (\cI \setminus \cA(x^*))
$$
de sorte que ces termes peuvent être omis.

\mbox{}

Mais qu'en est-il des indices $j \in \cA(x^*) \cap \cI$?
Nous savons déjà que $\lambda_i^* d^T\nabla g_i(x^*) \leq 0,\ i \in \cA(x^*)$.


Nous allons distinguer deux cas.

\end{frame}

\begin{frame}
\frametitle{Conditions nécessaires d'optimalité au second ordre}

Cas 1: il existe un indice $j \in \cA(x^*)$ tel que $\lambda_j^* d^T \nabla g_j(x^*) < 0$.

\mbox{}

Alors, pour tout $0 < t \ll 1$,
\begin{align*}
f(x(t))
&= f(x^*) - t \sum_{i \in \cE \cup \cI} \lambda_i^* d^T \nabla g_i(x^*) + O(t^2) \\
&\geq f(x^*) - t \lambda_j^* d^T \nabla g_j(x^*) + O(t^2) \\
&> f(x^*)
\end{align*}
Dès lors, dans ce cas, $f$ est strictement croissante le long du chemin $x(t)$ pour $t$ suffisamment petit, même si $\frac{d^2}{dt^2} f(x(0))$ était négatif.
% En raison de la contrainte $g_j$, rien ne peut être dit à propos de $\nabla^2 f(x^*) d$.

\end{frame}

\begin{frame}[shrink=2]
\frametitle{Conditions nécessaires d'optimalité au second ordre}

\begin{columns}[T]
\begin{column}{0.52\textwidth}
Cas 2:
$$
\lambda_i^* d^T \nabla g_i(x^*) = 0,\ i \in \cE \cup \cI.
$$
L'argument de Cas 1 ne s'applique plus.
Nous savons seulement que $\frac{d}{dt} f(x(0)) = 0$: $x^*$ est un point stationnaire du problème restreint.

Mais il pourrait s'agir d'un maximiseur local ou d'un point selle!

Les dérivées secondes $\frac{d^2}{dt^2} f(x(0))$ déterminent si $t=0$ est un minimiseur local: si cette quantité est strictement négative, alors $x^*$ n'est pas un minimum local le long de ce chemin. Il reste à relier $\frac{d^2}{dt^2} f(x(0))$ au Hessien du Lagrangien.
\end{column}
\begin{column}{0.45\textwidth}
\centering
\begin{tikzpicture}[scale=1.1,>=Stealth]
  \draw[->] (-0.15,0)--(2.5,0) node[right,font=\tiny] {$t$};
  \draw[->] (0,-1.0)--(0,1.2) node[above,font=\tiny] {$f(x(t))\!-\!f(x^*)$};
  % Minimum: f'' > 0, e.g. (t-0)^2 * 0.6
  \draw[blue,thick,domain=0:2.2,smooth]
    plot (\x,{0.5*\x*\x - 0.9*\x*\x*\x*0.12})
    node[right,font=\tiny,blue] {min ($f''\!>\!0$)};
  % Maximum: f'' < 0
  \draw[red,thick,domain=0:2.2,smooth]
    plot (\x,{-0.45*\x*\x + 0.9*\x*\x*\x*0.12})
    node[right,font=\tiny,red] {max ($f''\!<\!0$)};
  % Saddle: f = t^3 (f''=0)
  \draw[orange!80!black,thick,domain=0:1.6,smooth]
    plot (\x,{0.25*\x*\x*\x - 0.06*\x*\x*\x*\x})
    node[right,font=\tiny,orange!80!black] {selle ($f''\!=\!0$)};
  % x*
  \fill[black] (0,0) circle (2pt) node[below left,font=\tiny] {$x^*$};
  % Horizontal guide
  \draw[gray!50,dashed,thin] (0,0)--(2.3,0);
\end{tikzpicture}

{\small\centering Profils de $f(x(t))$ pour $t \geq 0$\\ selon le signe de $\frac{d^2}{dt^2}f(x(0))$}
\end{column}
\end{columns}

\end{frame}

\begin{frame}
	\frametitle{Conditions nécessaires d'optimalité au second ordre}
	
Nous noterons
$$
N^+ =
\left\lbrace d \ne 0 \,\bigg|\,
\begin{matrix}
d^T \nabla g_i(x^*) = 0, & i \in \cE \cup \{ j \in \cA(x^*) \cap \cI \,|\, \lambda_j^* > 0 \} \\
d^T \nabla g_i(x^*) \leq 0, & \{ i \in \cA(x^*) \cap \cI \,|\, \lambda_i^* = 0 \} 
\end{matrix}
\right\rbrace
$$
Dès lors, $d \in N^+$ implique $\lambda_i^* d^T \nabla g_i(x^*) = 0$, $i \in \cE \cup \cI$.

%\end{frame}

%\begin{frame}
%\frametitle{Conditions nécessaires d'optimalité au second ordre}

\mbox{}

\begin{thm}[Conditions nécessaires d'optimalité au second ordre]
Soit $x^*$ un minimiseur local du PNL où la LICQ tient.
Soit $\lambda^* \in \RR^m$ un vecteur de multiplicateurs de Lagrange tel que $(x^*, \lambda^*)$ satisfaisant les conditions KKT.
Alors nous avons
$$
d^T \nabla^2_{xx} L(x^*, \lambda^*) d \geq 0
$$
pour tout $d \in N^+$.
\end{thm}

\end{frame}

\begin{frame}
\frametitle{Géométrie de $N^+$}

% Figure en flottant absolu, coin supérieur droit
\begin{textblock*}{6.0cm}(9.6cm,1.55cm)
\centering
\begin{tikzpicture}[scale=1.15,>=Stealth]
  \fill[blue!10] (-1.5,0) rectangle (1.5,1.6);
  \draw[blue!60!black,thick] (-1.5,0)--(1.5,0)
    node[right,font=\tiny] {$g(x)=0$};
  \fill[red] (0,0) circle (1.8pt) node[below,font=\small,red] {$x^*$};
  \draw[->,thick,orange!80!black] (0,0)--(0,-0.55)
    node[below,font=\tiny,orange!80!black] {$\nabla g_j$};
  \draw[green!50!black,very thick,<->] (-1.3,0)--(1.3,0);
  \node[green!60!black,font=\tiny,above] at (0.9,0) {$N^+\ (\lambda^*\!>\!0)$};
  \fill[green!20,opacity=0.45] (0,0) -- (-1.4,1.35) -- (1.4,1.35) -- cycle;
  \draw[->,green!40!black,thick] (0,0)--(1.1,1.05)
    node[right,font=\tiny,green!40!black] {$N^+\!=\!T_{\cX}$};
  \draw[->,green!40!black,thick] (0,0)--(-1.1,1.05);
  \node[blue!60!black,font=\small] at (-1.1,0.8) {$\mathcal{X}$};
  \node[green!30!black,font=\tiny] at (0,0.5) {$(\lambda^*\!=\!0)$};
\end{tikzpicture}
\end{textblock*}

% Bloc de texte limité en largeur pour ne pas chevaucher la figure
\begin{minipage}[t]{0.56\textwidth}
Sous LICQ, le cône tangent à $\cX$ en $x^*$ est:
$$
T_{\cX}(x^*) = \left\{ d \,\middle|\,
\begin{aligned}
&d^T\nabla g_i = 0,\ i \in \cE \\
&d^T\nabla g_i \leq 0,\ i \in \cA(x^*)\cap\cI
\end{aligned}
\right\}
$$
En comparant avec la définition de $N^+$ terme à terme: tout $d \in N^+$ satisfait $d^T\nabla g_i = 0$ pour $i\in\cE$ et $d^T\nabla g_i \leq 0$ pour $i \in \cA(x^*)\cap\cI$, donc $d \in T_{\cX}(x^*)$.

Ainsi $\boldsymbol{N^+ \subseteq T_{\cX}(x^*)}$.
\end{minipage}

\medskip

% Texte en pleine largeur, sous la figure
Sous complémentarité stricte ($\lambda_j^*\ne 0$ pour tout $j\in\cA(x^*)\cap\cI$), toutes les contraintes actives imposent $d^T\nabla g_j = 0$: les deux ensembles coïncident et $N^+ = T_{\cX}(x^*)$.

\end{frame}

\begin{frame}
\frametitle{Preuve}

Soit $d \in N^+$ et soit $x(\cdot) \in C^2((-\epsilon, \epsilon), \RR^n)$ un chemin de sortie réalisable à partir de $x^*$ correspondant à $d$ (dont l'existence est garantie par la LICQ).

Par le Lemme sur la LICQ, $g_i(x(t)) = t d^T \nabla g_i(x^*)$ pour $i \in \cE \cup \cA(x^*)$. Pour $i \in \cI \setminus \cA(x^*)$, la complémentarité KKT donne $\lambda_i^* = 0$. Enfin, $d \in N^+$ implique $\lambda_i^* d^T \nabla g_i(x^*) = 0$ pour tout $i \in \cE \cup \cI$. Dès lors:
\begin{align*}
L(x(t), \lambda^*) &= f(x(t)) + \sum_{i \in \cE \cup \cI} \lambda_i^* g_i(x(t))\\
&= f(x(t)) + \sum_{i \in \cE \cup \cI} \lambda_i^* \cdot t d^T \nabla g_i(x^*)
  \quad \text{(Lemme LICQ + complémentarité)}\\
&= f(x(t)) + t \sum_{i \in \cE \cup \cI} \lambda_i^* d^T \nabla g_i(x^*)
  = f(x(t))
  \quad \text{(car $d \in N^+$)}
\end{align*}

\end{frame}

\begin{frame}
\frametitle{Preuve: règle de la chaîne au second ordre}

Posons $h(t) := L(x(t), \lambda^*) = f(x(t))$. Par la règle de la chaîne:
$$
\frac{d}{dt}h(t) = \nabla_x L(x(t), \lambda^*)^T \frac{d}{dt}x(t)
$$
En dérivant une seconde fois:
\begin{align*}
\frac{d^2}{dt^2}h(t) &= \left(\frac{d}{dt}x(t)\right)^T \nabla_{xx}^2 L(x(t), \lambda^*)\, \frac{d}{dt}x(t) \\
&\quad + \nabla_x L(x(t), \lambda^*)^T \frac{d^2}{dt^2}x(t)
\end{align*}
En $t = 0$, avec $\frac{d}{dt}x(0) = d$:
\begin{align*}
\frac{d}{dt}h(0) &= \nabla_x L(x^*, \lambda^*)^T d \\
\frac{d^2}{dt^2}h(0) &= d^T \nabla_{xx}^2 L(x^*, \lambda^*)\, d + \nabla_x L(x^*, \lambda^*)^T \frac{d^2}{dt^2}x(0)
\end{align*}

\end{frame}

\begin{frame}
\frametitle{Preuve: développement de Taylor au second ordre}

Le développement de Taylor
$$
h(t) = h(0) + t\, \frac{d}{dt}h(0) + \frac{t^2}{2}\, \frac{d^2}{dt^2}h(0) + O(t^3)
$$
donne donc
\begin{align*}
f(x(t)) = &\ L(x^*, \lambda^*) + t \nabla_x L(x^*, \lambda^*)^T d \\
& + \frac{t^2}{2}\!\left( d^T \nabla_{xx}^2 L(x^*, \lambda^*) d
  + \nabla_x L(x^*, \lambda^*)^T \frac{d^2}{dt^2}x(0) \right)
  + O(t^3)
\end{align*}

\end{frame}

\begin{frame}
\frametitle{Preuve}

Les conditions KKT impliquent
$$
\nabla_x L(x^*, \lambda^*) = 0
$$
Dès lors
$$
f(x(t)) = L(x^*, \lambda^*) + \frac{t^2}{2} d^T \nabla_{xx}^2 L(x^*, \lambda^*) d + O(t^3)
$$
et, des conditions KKT, $L(x^*, \lambda^*) = f(x^*) + \sum_{i \in \cE \cup \cI} \lambda_i^* g_i(x^*) = f(x^*)$,
puisque $g_i(x^*) = 0$ pour $i \in \cE$ et $\lambda_i^* g_i(x^*) = 0$ pour $i \in \cI$ (complémentarité)

\mbox{}

Si $d^T \nabla_{xx}^2 L(x^*, \lambda^*) d < 0$ alors pour $t$ suffisamment petit, $f(x(t)) < f(x^*)$, contredisant l'hypothèse que $x^*$ est un minimiseur local.

\mbox{}

Dès lors, nous devons avoir
$$
d^T \nabla_{xx}^2 L(x^*, \lambda^*) d \geq 0
$$

\end{frame}

\begin{frame}
\frametitle{Conditions suffisantes d'optimalité au second ordre}

\begin{thm}[Conditions suffisantes d'optimalité au second ordre]
Soit $(x^*, \lambda^*) \in \RR^n \times \RR^m$ satisfaisant les conditions KKT, et sous une hypothèse de qualification de contrainte, si
$$
d^T \nabla^2_{xx} L(x^*, \lambda^*)d > 0
$$
pour toute  direction $d \in N^+$.
Alors $x^*$ est un minimiseur local strict.
\end{thm}

\end{frame}

\begin{frame}
\frametitle{Preuve}

Supposons par contradiction que $x^*$ n'est pas un minimiseur local.
Il existe alors une séquence de points réalisables $\{ x_k \}$, $k = 1,\ldots$, telle que
$x_k \ne x^*$, $\lim_{k \rightarrow \infty} x_k = x^*$ et $f(x_k) \leq f(x^*)$ pour tout $k \in \NN$.

\mbox{}

La séquence $d_k := \frac{x_k-x^*}{\| x_k-x^* \|}$ tient sur la sphère unité, qui est un ensemble compact.
On peut donc en extraire une sous-séquence $\{ d_{k_i} \}$ convergeant vers une direction limite $d$, avec $d \ne 0$.
Sans perte de généralité, on pose $k_i = i$.

\end{frame}

\begin{frame}
\frametitle{Preuve}

Vérifions que $d \in N^+$. Considérons tout d'abord les contraintes d'égalité.
$\forall i \in \cE$, en utilisant le développement de Taylor, nous avons
\begin{align*}
0 &= \frac{g_i(x_k)}{\| x_k - x^* \| } \\
&= \frac{1}{\| x_k - x^* \| } \left( g_i(x^*) + \| x_k - x^* \| \nabla g_i(x^*)^Td_k + O(\| x_k - x^* \|^2) \right) \\
&= \nabla g_i(x^*)^Td_k + O(\| x_k - x^* \|)
\end{align*}
En faisant tendre $k$ vers l'infini, nous obtenons
$$
\nabla g_i(x^*)^Td = 0
$$

\end{frame}

\begin{frame}
\frametitle{Preuve}

$\forall i \in \cA(x^*) \cap \cI$, en utilisant le développement de Taylor, nous avons de manière similaire
\begin{align*}
0 &\geq \frac{g_i(x_k)}{\| x_k - x^* \| } \\
&= \frac{1}{\| x_k - x^* \| } \left( g_i(x^*) + \| x_k - x^* \| \nabla g_i(x^*)^Td_k + O(\| x_k - x^* \|^2) \right) \\
&= \nabla g_i(x^*)^Td_k + O(\| x_k - x^* \|)
\end{align*}
En faisant tendre $k$ vers l'infini, nous obtenons
$$
\nabla g_i(x^*)^Td \leq 0
$$

\mbox{}

Ainsi, $d$ est une direction de sortie réalisable. Montrons que $d \in N^+$.

\end{frame}

\begin{frame}
\frametitle{Preuve}

Si $d \notin N^+$, alors $\exists j \in \cA(x^*) \cap \cI$ tel que
$$
\lambda_j^* \nabla g_j(x^*)^Td < 0
$$
En vertu du développement de Taylor,
\begin{align*}
\lambda_j^* g_j(x_k)
&= \lambda_j^* g_j(x^*) + \lambda_j^* \| x_k - x^* \| \nabla g_j(x^*)^Td_k + O(\| x_k - x^* \|^2) \\
&= \lambda_j^* \| x_k - x^* \|\nabla g_j(x^*)^Td_k + O(\| x_k - x^* \|^2)
\end{align*}
Dès lors
\begin{align*}
L(x_k, \lambda^*) &= f(x_k) + \sum_{i \in \cE \cup \cI} \lambda_i^* g_i(x_k) \\
&\leq f(x_k) + \lambda_j^* g_j(x_k) \\ 
&=  f(x_k) + \lambda_j^*\| x_k - x^* \|\nabla g_j(x^*)^Td_k + O(\| x_k - x^* \|^2)
\end{align*}

\end{frame}

\begin{frame}
\frametitle{Preuve}

D'autre part, le développement de Taylor de $L(x_k,\lambda^*)$ autour de $(x^*, \lambda^*)$ donne
\begin{align*}
L(x_k,\lambda^*) =\ & L(x^*,\lambda^*) + \| x_k - x^* \| d_k^T \nabla_x L(x^*,\lambda^*)
\\ & + \frac{ \| x_k - x^* \|^2 }{2} d_k^T \nabla^2_{xx} L(x^*,\lambda^*) d_k + O( \| x_k - x^* \|^3 )
\end{align*}
De part les conditions KKT, $\nabla_x L(x^*,\lambda^*) = 0$ et $L(x^*, \lambda^*) = f(x^*)$.
Dès lors, nous obtenons
$$
L(x_k,\lambda^*) = f(x^*) + \frac{ \| x_k - x^* \|^2 }{2} d_k^T \nabla^2_{xx} L(x^*,\lambda^*) d_k + O( \| x_k - x^* \|^3 )
$$

\mbox{}

En combinant ces expressions,
\begin{align*}
f(x_k) & \geq L(x_k, \lambda^*) - \lambda_j^*\| x_k - x^* \|\nabla g_j(x^*)^Td_k + O(\| x_k - x^* \|^2) \\
&= f(x^*) - \lambda_j^*\| x_k - x^* \|\nabla g_j(x^*)^Td_k + O(\| x_k - x^* \|^2)
\end{align*}
et pour $k$ assez grand, $f(x_k) > f(x^*)$ contredisant nos hypothèses de travail.
\end{frame}

\begin{frame}
\frametitle{Preuve}

Ainsi $d \in N^+$, et des hypothèses du théorème, nous avons 
$$
d^T \nabla_{xx}^2 L(x^*, \lambda^*) d > 0.
$$

%Le développement de Taylor de $f(x_k)$ autour de $x^*$ donne

%$$
%f(x_k) = f(x^*) + (x_k-x^*)^T\nabla f(x^*) + O(\| x_k-x^* \|^2)
%$$
%que nous pouvons réécrire, en utilisant la définition de $d_k$,

%$$
%f(x_k) = f(x^*) + \| x_k-x^* \| \nabla f(x^*)^Td_k + O(\| %x_k-x^* \|^2)
%$$
%Par hypothèse, $f(x^*) \geq f(x_k)$, et dès lors%, pour $k$ assez grand,
%$$
%\nabla f(x^*)^Td = \lim_{k \rightarrow \infty} \nabla f(x^*)^Td_k \leq 0
%$$
D'autre part, des conditions KKT et $d \in N^+$,
$$
\nabla f(x^*)^Td = -d^T\sum_{i \in \cE \cup \cI} \lambda_i^* \nabla g_i(x^*)
= \sum_{i \in \cE \cup \cI} \lambda_i^* (-d^T \nabla g_i(x^*)) = 0
$$
De plus, de nos hypothèses de départ et des conditions KKT,
$$
f(x^*) \geq f(x_k) \geq f(x_k) + \sum_{i \in \cE \cup \cI} \lambda_i^* g_i(x_k) = L(x_k,\lambda^*)
$$
et, reprenant le développement de $L(x_k,\lambda^*)$
$$
f(x^*) \geq f(x^*) + \frac{ \| x_k - x^* \|^2 }{2} d_k^T \nabla^2_{xx} L(x^*,\lambda^*) d_k + O( \| x_k - x^* \|^3 )
$$
\end{frame}

\begin{frame}
\frametitle{Preuve}

Il s'ensuit
$$
d_k^T \nabla^2_{xx} L(x^*,\lambda^*) d_k \leq O( \| x_k - x^* \| )
$$
en passant à la limite comme $k \rightarrow \infty$
$$
d^T \nabla^2_{xx} L(x^*,\lambda^*) d \leq 0
$$
Ceci contredit l'hypothèse du théorème
$$
d^T \nabla^2_{xx} L(x^*,\lambda^*) d > 0
$$
et donc l'hypothèse d'existence de la séquence $\{ x_k \}$ ne tient pas. Dès lors, $x^*$ est minimiseur local.

\end{frame}

%\begin{frame}
%\frametitle{Preuve}

%Combinant les deux inégalités, nous devons avoir
%$$
%\nabla f(x^*)^Td = 0
%$$

%\mbox{}

%D'autre part, de nos hypothèses de départ et des conditions KKT,
%$$
%f(x^*) \geq f(x_k) \geq f(x_k) + \sum_{i \in \cE \cup \cI} \lambda_i^* g_i(x_k) = L(x_k,\lambda^*)
%$$

%\end{frame}

\begin{frame}
\frametitle{Complémentarité stricte}

Il est courant, et commode, de supposer que la condition de complémentarité stricte s'applique. Celle-ci requiert que le multiplicateur de Lagrange ne peut s'annuler pour une contrainte active, i.e.
$$
\lambda_j^* \ne 0\ \forall j \in \cA(x^*) \cap \cI
$$

\mbox{}

La complémentarité stricte permet de réexprimer $N^+$ de manière très commode. Rappelons que
$$
N^+ =
\left\lbrace d \ne 0 \,\bigg|\,
\begin{matrix}
d^T \nabla g_i(x^*) = 0, & i \in \cE \cup \{ j \in \cA(x^*) \cap \cI \,|\, \lambda_j^* > 0 \} \\
d^T \nabla g_i(x^*) \leq 0, & \{ i \in \cA(x^*) \cap \cI \,|\, \lambda_i^* = 0 \} 
\end{matrix}
\right\rbrace
$$

%Un des avantages de la complémentarité stricte est qu'il est possible d'identifier l'ensemble actif en $x^*$ à proximité de $x^*$. En effet, pour $\epsilon > 0$ suffisamment petit, par continuité, si $x_k \in \cB(x^*, \epsilon)$, $g(x_k) $

sous la complémentarité stricte, nous avons
$$
N^+ =
\left\lbrace d \ne 0 \,|\,
d^T \nabla g_i(x^*) = 0, i \in \cA(x^*)
\right\rbrace
$$

\end{frame}

\begin{frame}
\frametitle{Complémentarité stricte}

Autrement dit, $d$ appartient au noyau du Jacobien des contraintes actives:
$$
d \in \ker(J),\ J = \begin{pmatrix} \nabla g_i(x^*)^T, i \in \cA(x^*) \end{pmatrix}
$$

\mbox{}

Comme
$$
d \in N^+
\Rightarrow
\lambda_i^* d^T \nabla g_i(x^*) = 0,\ i \in \cE \cup \cI,
$$
nous avons également
$$
d \in N^+
\Leftrightarrow
\lambda_i^* d^T \nabla g_i(x^*) = 0,\ i \in \cE \cup \cI,
$$

\end{frame}

\end{document}