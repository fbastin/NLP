\documentclass[usepdftitle=false,aspectratio=169]{beamer}

\usepackage[utf8]{inputenc}
\usetheme{Singapore}
\usepackage{xcolor}
\setbeamertemplate{footline}[frame number]

\title[IFT3515]{IFT 3515\\Fonctions à plusieurs variables\\Optimisation avec contraintes\\Conditions d'optimalité}
\author[Fabian Bastin]{Fabian Bastin\\DIRO\\Université de Montréal}
\date{}

\usepackage{enumerate}
\usepackage[french]{babel}

\usepackage{easybmat}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{patterns,arrows.meta,decorations.markings,decorations.pathreplacing,calc,positioning}

\newtheorem{defn}{Définition}
\newtheorem{lem}{Lemme}
\newtheorem{thm}{Théorème}
\newtheorem{coro}{Corollaire}

\def\red{\color{red}}
\def\blue{\color{blue}}

\def\cA{\mathcal{A}}
\def\cB{\mathcal{B}}
\def\cE{\mathcal{E}}
\def\cI{\mathcal{I}}
\def\cL{\mathcal{L}}
\def\cN{\mathcal{N}}
\def\cR{\mathcal{R}}
\def\cX{\mathcal{X}}
\def\cV{\mathcal{V}}

\def\bu{\boldsymbol{u}}

\def\RR{\mathbb{R}}

\renewcommand{\proofname}{Preuve}

\begin{document}
\frame{\titlepage}

% ------------------------------------------------------------------------------------------------------------------------------------------------------

\begin{frame}
	\frametitle{Lagrangien et fonction duale lagrangienne}
	
	Considérons à présent un problème ayant des contraintes d'égalité et d'inégalité:
	\begin{align*}
		\min_{x \in \mathbb{R}^n} \ & f(x) \\
		\mbox{t.q. } & g_i(x) \leq 0,\ i = 1,\ldots,m, \\
		& h_j(x) = 0,\ j = 1,\ldots,r.
	\end{align*}
	
	Nous définissons le Lagrangien comme
	$$
	L(x, \lambda, \mu) = f(x) + \sum_{i = 1}^m \lambda_i g_i(x)
	+ \sum_{j = 1}^r \mu_j h_j(x)
	$$
	et la fonction duale lagrangienne
	$$
	\cL(\lambda, \mu) = \min_{x \in \mathbb{R}^n} L(x, \lambda, \mu).
	$$
	
\end{frame}

\begin{frame}
\frametitle{Multiplicateurs de Lagrange: contraintes d'égalité}

Considérons le problème de programmation mathématique suivant
\begin{align*}
\min_{x \in \cX} \ & f(x) \\
\mbox{t.q. } & g_i(x) = 0,\ i = 1,\ldots,m,
\end{align*}
où $\cX \subset \RR^n$, $f: \RR^n \rightarrow \RR$, $g_i: \RR^n \rightarrow \RR$, $i = 1,\ldots,m$.

\mbox{}

Le {\red Lagrangien} associé à ce problème est obtenu en associant un multiplicateur de Lagrange $\lambda_i$ à chaque fonction de contrainte $g_i$:
$$
L(x, \lambda) = f(x) + \sum_{i = 1}^{m} \lambda_i g_i(x).
$$

Sans faire d'hypothèse particulière sur $\cX$ ou sur les fonctions $f$ et $g_i$, nous pouvons obtenir des conditions très générales pour qu'un point $x^*$ soit une solution optimale du problème.

\end{frame}

\begin{frame}
\frametitle{Optimalité}

\begin{thm}
Supposons que le Lagrangien associé au problème
\begin{align*}
\min_{x \in \cX} \ & f(x) \\
\mbox{t.q. } & g_i(x) = 0,\ i = 1,\ldots,m.
\end{align*}
possède un minimum local $x^* \in \cX$ lorsque le vecteur de multiplicateurs $\lambda$ vaut $\lambda^*$.
Si $g_i(x^*) = 0$, $i = 1,\ldots,m$, alors $x^*$ est un minimum local de $f(x)$.
\end{thm}

\end{frame}

\begin{frame}
\frametitle{Optimalité}

\begin{proof}
	La preuve se fait par contradiction en supposant que $x^*$ n'est pas un minimum local de $f(x)$.
	Alors $\forall \epsilon > 0$, $\exists\, \overline{x} \in \cB(x^*, \epsilon)$ tel que $g_i(\overline{x}) = 0$, $i = 1,\ldots,m$ et $f(\overline{x}) < f(x^*)$.
	
Par conséquent, pour tout $\lambda$,
	$$
	\sum_{i = 1}^{m} \lambda_i g_i(x^*) = \sum_{i = 1}^{m} \lambda_i g_i(\overline{x}) = 0.
	$$
	Dès lors,
$$
f(\overline{x}) + \sum_{i = 1}^{m} \lambda_i g_i(\overline{x}) < f(x^*) + \sum_{i = 1}^{m} \lambda_i g_i(x^*).
$$
En prenant $\lambda = \lambda^*$, la relation précédente contredit le fait que $x^*$ est un
minimum local du Lagrangien lorsque $\lambda = \lambda^*$.
\end{proof}

\end{frame}

\begin{frame}
\frametitle{Multiplicateurs de Lagrange: contraintes d'inégalité}

Considérons le problème de programmation mathématique suivant
\begin{align*}
\min_{x \in \cX} \ & f(x) \\
\mbox{t.q. } & g_i(x) \leq 0,\ i = 1,\ldots,m.
\end{align*}
où $\cX \subset \RR^n$, $f: \RR^n \rightarrow \RR$, $g_i: \RR^n \rightarrow \RR$, $i = 1,\ldots,m$.

\mbox{}

\begin{thm}
Supposons que le Lagrangien associé au problème
\begin{align*}
\min_{x \in \cX} \ & f(x) \\
\mbox{t.q. } & g_i(x) \leq 0,\ i = 1,\ldots,m,
\end{align*}
possède un minimum local $x^* \in \cX$ lorsque le vecteur de multiplicateurs $\lambda$ vaut $\lambda^*$.
Si $g_i(x^*) \leq 0$, $\lambda^*_i \geq 0$ et $\lambda^*_i g_i(x^*) = 0$, $i = 1,\ldots,m$, alors $x^*$ est un minimum local de $f(x)$.
\end{thm}

\end{frame}

\begin{frame}
\frametitle{Multiplicateurs de Lagrange: contraintes d'inégalité}
	
\begin{proof}
Comme précédemment, la preuve se fait par contradiction en supposant que $x^*$ n'est pas un minimum local de $f(x)$.
Alors $\forall \epsilon > 0$, $\exists\, \overline{x} \in \cB(x^*, \epsilon)$ tel que $g_i(\overline{x}) \leq 0$, $i = 1,\ldots,m$ et $f(\overline{x}) < f(x^*)$.
Par conséquent, pour $\lambda = \lambda^* \geq 0$,
$$
\sum_{i = 1}^{m} \lambda_i g_i(\overline{x}) \leq 0 \mbox{ et }
\sum_{i = 1}^{m} \lambda_i g_i(x^*) = 0.
$$
Dès lors,
$$
f(\overline{x}) + \sum_{i = 1}^{m} \lambda_i g_i(\overline{x}) < f(x^*) + \sum_{i = 1}^{m} \lambda_i g_i(x^*).
$$
La relation précédente contredit le fait que $x^*$ est un
minimum local du Lagrangien lorsque $\lambda = \lambda^*$.
\end{proof}

\end{frame}

\begin{frame}
\frametitle{Problème dual}

Le problème dual est
\begin{align*}
\max_{\lambda \in \RR^m, \mu \in \RR^r}\ & \cL(\lambda, \mu) \\
\mbox{tel que } & \lambda \geq 0.
\end{align*}

Propriétés importantes:
\begin{itemize}
\item
Le problème dual est toujours convexe, i.e. $\cL$ est toujours concave (même si le problème primal n'est pas convexe).
\item
Les valeurs optimales (globales) primale et duale, $f^*$ et $\cL^*$, satisfont toujours la dualité faible: $f^* \geq \cL^*$.
\item {\blue Dualité forte}: sous certaines conditions (qualifications de contraintes), $f^* = \cL^*$.
\end{itemize}

\end{frame}

\begin{frame}
\frametitle{Saut de dualité}

Étant donné une solution primale réalisable $x$ et une solution duale réalisable $(\lambda, \mu)$, la quantité $f(x) - \cL(\lambda, \mu)$ est appelée le \textcolor{red}{saut de dualité} entre $x$ et $(\lambda, \mu)$.
Notons que
$$
f(x) - f^* \leq f(x) - \cL(\lambda, \mu)
$$
de sorte que si le saut de dualité est nul, alors $x$ est optimal primal (et de même, $\lambda$ et $\mu$ sont optimaux duaux).

\mbox{}

D'un point de vue algorithmique, si la dualité forte tient, ceci fournit un critère d'arrêt: si $f(x) - \cL(\lambda, \mu) \leq \epsilon$, nous avons alors la garantie que $f(x) - f^* \leq \epsilon$.

\end{frame}

\begin{frame}
\frametitle{Saut de dualité: illustration}

\begin{center}
\begin{tikzpicture}[scale=0.82, every node/.style={transform shape}]

% Vertical axis
\draw[->, thick, gray!50] (0,-0.3) -- (0,7.2);
\node[above, gray, font=\small] at (0,7.2) {valeur};

% Dual region shading
\fill[blue!6] (-0.6, -0.1) rectangle (0.6, 2.5);
% Primal region shading
\fill[red!6] (-0.6, 4.0) rectangle (0.6, 6.8);

% L(lambda, mu) - dual feasible
\draw[thick, blue!60!black] (-0.4, 0.8) -- (0.4, 0.8);
\node[left=8pt, blue!60!black, font=\small] at (-0.4, 0.8) {$\cL(\lambda, \mu)$};

% L*
\draw[very thick, blue!70!black] (-0.55, 2.5) -- (0.55, 2.5);
\node[left=8pt, blue!70!black, font=\small\bfseries] at (-0.55, 2.5) {$\cL^*$};

% f*
\draw[very thick, red!70!black] (-0.55, 4.0) -- (0.55, 4.0);
\node[left=8pt, red!70!black, font=\small\bfseries] at (-0.55, 4.0) {$f^*$};

% f(x) - primal feasible
\draw[thick, red!60!black] (-0.4, 5.8) -- (0.4, 5.8);
\node[left=8pt, red!60!black, font=\small] at (-0.4, 5.8) {$f(x)$};

% Duality gap brace
\draw[decorate, decoration={brace, amplitude=8pt, mirror}, very thick, black!60!green]
  (0.85, 2.5) -- (0.85, 4.0);
\node[right=12pt, black!60!green, font=\small, align=left] at (0.85, 3.25) 
  {saut de dualité\\$f^* - \cL^* \geq 0$};

% Sub-optimality bound brace
\draw[decorate, decoration={brace, amplitude=7pt, mirror}, thick, orange!80!black]
  (4.8, 2.5) -- (4.8, 5.8);
\node[right=11pt, orange!80!black, font=\small, align=left] at (4.8, 4.15) 
  {$f(x) - \cL(\lambda,\mu)$\\$\geq f(x) - f^*$};

% Upward arrows between levels
\draw[-{Stealth[length=4pt]}, gray!40, thick] (0, 0.1) -- (0, 0.65);
\draw[-{Stealth[length=4pt]}, gray!40, thick] (0, 1.1) -- (0, 2.35);
\draw[-{Stealth[length=4pt]}, gray!40, thick] (0, 4.15) -- (0, 5.65);
\draw[-{Stealth[length=4pt]}, gray!40, thick] (0, 6.0) -- (0, 6.8);

% Annotations
\node[blue!60!black, font=\scriptsize, align=center] at (0, -0.7) {dualité faible: $\cL^* \leq f^*$ (toujours)};
\node[red!60!black, font=\scriptsize, align=center] at (5.5, 6.6) {dualité forte: $f^* = \cL^*$};

\end{tikzpicture}
\end{center}

\end{frame}

\begin{frame}
\frametitle{Saut de dualité: cas local}

Désignons l'ensemble réalisable par
$$
\cX = \{ x \,|\, g_i(x) \leq 0, i = 1,\ldots,m, h_j(x) = 0, j = 1,\ldots,r \}.
$$
Considérons $x^*$ un minimum local de $f(\cdot)$, i.e.
$$
\exists \epsilon > 0  \text{ t.q. } \forall x \in \cB(x^*, \epsilon) \cap \cX, f(x^*) \leq f(x).
$$
Nous pouvons également définir la fonction duale lagrangienne restreinte à la boule $\cB(x^*, \epsilon)$:
$$
\cL_{\cB(x^*, \epsilon)}(\lambda, \mu) = \min_{x \in \cB(x^*, \epsilon)} L(x,\lambda, \mu).
$$
Dans ce cas, la dualité faible tient toujours localement:
$$
\cL^*_{\cB(x^*, \epsilon)} \leq f(x^*).
$$
Sous certaines conditions, la dualité forte tient également:
$$
\cL^*_{\cB(x^*, \epsilon)} = f(x^*).
$$

\end{frame}

\begin{frame}
\frametitle{Saut de dualité: cas local}

Remarquons cependant que
$$
\min_{x \in \mathbb{R}^n} L(x, \lambda, \mu) 
\leq \min_{x \in \cB(x^*, \epsilon)} L(x, \lambda, \mu) 
$$
et donc
$$
\cL^* \leq \cL^*_{\cB(x^*, \epsilon)}.
$$

\mbox{}

Dès lors, si $x^*$ est un minimum local et si la dualité forte tient localement,
$$
\cL^* \leq f(x^*),
$$
l'inégalité pouvant être stricte.

\end{frame}

\begin{frame}
\frametitle{Conditions de Karush-Kuhn-Tucker (KKT)}

Soient $f, g_i, h_j \in C^1$, $i = 1,\ldots,m$, $j = 1,\ldots,r$, et le problème
\begin{align*}
\min_{x \in \mathbb{R}^n} \ & f(x) \\
\mbox{t.q. } & g_i(x) \leq 0,\ i = 1,\ldots,m, \\
& h_j(x) = 0,\ j = 1,\ldots,r.
\end{align*}

Conditions de Karush-Kuhn-Tucker (KKT):
\begin{align*}
\nabla_x L(x,\lambda,\mu) &= 0 & \mbox{(stationarité)}\\
\lambda_i g_i(x) &= 0 & \mbox{(écarts de complémentarité)} \\
g_i(x) &\leq 0,\ h_j(x) = 0,\ \forall i,j & \mbox{(faisabilité primale)} \\
\lambda_i &\geq 0\ \forall i & \mbox{(faisabilité duale)}
\end{align*}

\end{frame}

\begin{frame}
\frametitle{Conditions KKT: interprétation géométrique}

\begin{center}
\begin{tikzpicture}[scale=0.68, every node/.style={transform shape},
  constraint/.style={thick},
  gradient/.style={-{Stealth[length=6pt]}, thick},
  point/.style={circle, fill=red!80!black, inner sep=2.2pt},
]

% Axes
\draw[->,gray!50] (-0.5,0) -- (7,0) node[right,gray] {$x_1$};
\draw[->,gray!50] (0,-0.5) -- (0,6.5) node[above,gray] {$x_2$};

% Feasible region: triangle (3,3)-(1,1)-(5,1)
\fill[green!10, opacity=0.7] (3,3) -- (1,1) -- (5,1) -- cycle;
\node[green!40!black, font=\small] at (3,1.6) {$\cX$};

% Constraint lines
\draw[constraint, blue!70!black] (-0.2,6.2) -- (6.2,-0.2)
  node[pos=0.15, above right, blue!70!black, font=\small] {$g_1(x) = 0$};
\draw[constraint, blue!50!red] (0,0) -- (5,5)
  node[pos=0.88, below right, blue!50!red, font=\small] {$g_2(x) = 0$};
\draw[constraint, gray!70, dashed] (-0.3,1) -- (6.5,1)
  node[pos=0.95, below, gray!70, font=\small] {$g_3(x) = 0$};

% Contour arcs of f(x) = (x1-3)^2 + (x2-5)^2, centered at (3,5)
\draw[thin, gray!50, dashed] (3,5) +(-160:2) arc(-160:-20:2);
\draw[thin, gray!50, dashed] (3,5) +(-155:2.83) arc(-155:-25:2.83);
\draw[thin, gray!50, dashed] (3,5) +(-148:3.61) arc(-148:-32:3.61);
\node[gray!60, font=\scriptsize] at (6.2,3.5) {courbes de};
\node[gray!60, font=\scriptsize] at (6.2,3.0) {niveau de $f$};

% Unconstrained minimizer
\node[circle, fill=gray!60, inner sep=1.5pt] at (3,5) {};
\node[right=2pt, gray!60, font=\scriptsize] at (3,5) {min $f$ (non réalisable)};

% Point x*
\node[point] at (3,3) {};
\node[below left=2pt, font=\bfseries] at (3,3) {$x^*$};

% Gradient of g1: (1,1) direction, scaled
\draw[gradient, red!70!black, dashed] (3,3) -- ++(0.85,0.85)
  node[right, font=\small, red!70!black] {$\nabla g_1(x^*)$};

% Gradient of g2: (-1,1) direction, scaled
\draw[gradient, orange!80!black, dashed] (3,3) -- ++(-0.85,0.85)
  node[left, font=\small, orange!80!black] {$\nabla g_2(x^*)$};

% -nabla f: (0, 4) direction, scaled to (0, 1.4)
\draw[gradient, red!70!black, very thick] (3,3) -- ++(0,1.4)
  node[right=2pt, font=\small\bfseries, red!70!black] {$-\nabla f(x^*)$};

% Stationarity annotation
\draw[<-, thin, gray] (3.15,4.1) -- (6,5.5)
  node[right, font=\small, text width=5cm, gray!70!black] {%
    $-\nabla f(x^*) = \lambda_1^* \nabla g_1(x^*) + \lambda_2^* \nabla g_2(x^*)$\\[2pt]
    $\lambda_1^*, \lambda_2^* > 0$ \quad (stationarité)};

% Complementarity annotation for g3
\draw[<-, thin, gray] (3,1) -- (6,0.3)
  node[right, font=\small, gray!70!black] {$g_3(x^*) < 0 \Rightarrow \lambda_3^* = 0$ (complémentarité)};

\end{tikzpicture}
\end{center}

\end{frame}

\begin{frame}
\frametitle{Nécessité}

Soient $x^*$ minimum pour $x \in \cB(x^*,\epsilon)$, $\epsilon > 0$, et $(\lambda^*, \mu^*)$ solution du dual si $x$ est restreint à $\cB(x^*,\epsilon)$, avec un saut de dualité nul (la dualité forte tient).
Alors
\begin{align*}
f(x^*) &= \cL(\lambda^*, \mu^*) \\
&= \min_{x \in \cB(x^*,\epsilon)} \left( f(x) + \sum_{i = 1}^m \lambda_i^* g_i(x) + \sum_{j = 1}^r \mu_j^* h_j(x) \right) \\
& \leq f(x^*) + \sum_{i = 1}^m \lambda_i^* g_i(x^*) + \sum_{j = 1}^r \mu_j^* h_j(x^*) \\
& \leq f(x^*)
\end{align*}
Dès lors, $x^*$ est un minimum de $L(x, \lambda^*, \mu^*)$ sur $\cB(x^*,\epsilon)$.

\mbox{}

Par conséquent, $\nabla_x L(x^*, \lambda^*, \mu^*) = 0$.

\mbox{}

Nous retrouvons les conditions de stationarité.

\end{frame}

\begin{frame}
\frametitle{Nécessité}

Nous devons aussi avoir $\sum_{i = 1}^m \lambda_i^* g_i(x^*) = 0$ puisque $\sum_{i = 1}^m \lambda_i^* g_i(x^*) \leq 0$.
Ceci implique que pour tout $i$, $\lambda_i^* g_i(x^*) = 0$.

\mbox{}

Nous retrouvons les conditions de complémentarité.

\mbox{}

Si $x^*$ est un minimum global, nous pouvons remplacer $\cB(x^*,\epsilon)$ par $\RR^n$.

\end{frame}

\begin{frame}
\frametitle{Nécessité}

\begin{thm}[Nécessité des conditions KKT]
Si $x^*$, $(\lambda^*, \mu^*)$ sont des solutions primale et duale avec un saut de dualité nul (i.e. la dualité forte tient),
alors $x^*$, $(\lambda^*, \mu^*)$ satisfont les conditions KKT.
\end{thm}

\mbox{}

Dès lors, l'hypothèse de dualité forte apparaît importante. Elle sera garantie sous certaines conditions.
\begin{itemize}
\item
Programme linéaire. La dualité forte tient toujours.
\item
Programme convexe. Condition de Slater: $\exists x$ tel que $g_i(x) < 0$, $i = 1,\ldots,m$ et $h_j(x) = 0$, $j = 1,\ldots,r$.
\item
Programme non-convexe.
Hypothèse de qualification de contraintes.
La plus courante, mais aussi la plus forte, est la condition d'indépendance linéaire des gradients à la solution.
\end{itemize}

\end{frame}

\begin{frame}
\frametitle{Nécessité (cas non convexe)}

\begin{thm}[Nécessité des conditions KKT]
Si $x^*$ est une solution locale de
\begin{align*}
\min_{x \in \cX} \ & f(x) \\
\mbox{t.q. } & g_i(x) \leq 0,\ i = 1,\ldots,m \\
& h_j(x) = 0,\ j = 1,\ldots,r,
\end{align*}
où les fonctions $f$, $g_i$ et $h_j$, $i = 1,\ldots,m$, $j = 1,\ldots,r$, sont continûment différentiables, et qu'une condition de qualification de contrainte tient en $x^*$.
Alors, il existe un vecteur de multiplicateurs de Lagrange  $(\lambda^*, \mu^*)$ tel que les conditions KKT sont satisfaites en $(x^*,\lambda^*, \mu^*)$.
\end{thm}

\begin{proof}
Preuve: technique!
Voir par exemple Nocedal \& Wright, ``Numerical Optimization'', Section 12.4.
\end{proof}

\end{frame}

\begin{frame}
	\frametitle{Suffisance des conditions KKT}
	
	S'il existe $x^*$, $(\lambda^*, \mu^*)$ satisfaisant les conditions KKT, alors
	\begin{align*}
	L(x^*, \lambda^*, \mu^*) &= f(x^*) + \sum_{i = 1}^m \lambda_i^* g_i(x^*) + \sum_{j = 1}^r \mu_j^* h_j(x^*) \\
	&= f(x^*)
	\end{align*}
	
	Dès lors, le saut de dualité est nul ({\red dualité forte}).
	% si $L(\lambda^*, \mu^*) = \cL(\lambda^*, \mu^*)$, ce qui est le cas si $x^*$ est un minimum global de $\cL(\lambda^*, \mu^*)$.
	
	\mbox{}
	
	Dans le cas convexe, cela implique que 	$x^*$ et $(\lambda^*, \mu^*)$ sont des solutions globales primale et duale, respectivement.

	\mbox{}

	Dans le cas non-convexe, $x^*$ est un minimum local, pas nécessairement global, voire un point-selle.
	% donner un exemple du dernier cas
	
	%Si $x^*$ n'est pas un minimum global, la fonction dual lagrangienne n'est pas non plus minimisée globalement. Si on considère le minimum global de la fonction duale lagrangienne, le saut de dualité n'est pas nul.
	
\end{frame}

\begin{frame}
	\frametitle{Contraintes linéaires}
	
	Revenons à la méthode de projection.
	Un autre cas important de contraintes relativement faciles à traiter sont les contraintes d'égalité
	$$
	Ax = b
	$$
	avec $A \in \RR^{m \times n}$ de rang plein.
	
	Nous pouvons généraliser le problème de projection de $y$ sur l'ensemble
	$$
	\cX = \lbrace x \,|\, Ax = b \rbrace
	$$
	en considérant la norme-2 ou la norme engendrée par une matrice $H$ définie positive
	$$
	\min_x \frac{1}{2} \| y - x \|_H^2 \mbox{ tel que } Ax = b.
	$$
	Nous allons résoudre ce problème en utilisant les conditions d'optimalité de Karush-Kuhn-Tucker (KKT).
	
\end{frame}

\begin{frame}
\frametitle{Projection sur un sous-espace affine: illustration}

\begin{center}
\begin{tikzpicture}[scale=0.85, every node/.style={transform shape},
  gradient/.style={-{Stealth[length=6pt]}, thick},
  point/.style={circle, fill=red!80!black, inner sep=2.2pt},
]

% Affine subspace (line Ax = b)
\draw[very thick, blue!70!black] (-0.5,0.75) -- (7.5,4.75)
  node[pos=0.92, above left, blue!70!black, font=\small] {$\cX = \{x \,|\, Ax = b\}$};

% Point y
\coordinate (y) at (3, 5.2);
\node[point, fill=red!70!black] at (y) {};
\node[above=3pt, font=\bfseries, red!70!black] at (y) {$y$};

% Projection p = P_X(y), computed: (3.8, 2.9)
\coordinate (p) at (3.8, 2.9);
\node[point, fill=green!60!black] at (p) {};
\node[below right=2pt, font=\bfseries, green!60!black] at (p) {$P_{\cX}(y)$};

% Orthogonal segment y -> p
\draw[thick, dashed, red!60!black] (y) -- (p);

% Right angle mark
\coordinate (along) at ($(p) + (0.4, 0.2)$);
\coordinate (perp) at ($(p) + (-0.16, 0.32)$);
\coordinate (corner) at ($(along) + (-0.16, 0.32)$);
\draw[thin, gray!70] (along) -- (corner) -- (perp);

% Label for orthogonality
\draw[<-, thin, gray] (3.3,4.0) -- (5.5,5.5)
  node[right, font=\small, text width=5cm, gray!70!black] {%
    $y - P_{\cX}(y) \perp \cX$\\[2pt]
    $\|y - P_{\cX}(y)\|_H = \min_{x \in \cX} \|y - x\|_H$};

% H-norm ball (ellipse) tangent to line at p
\draw[thin, gray!50, dashed] (y) ellipse (1.5 and 2.5);
\node[gray!50, font=\scriptsize, right] at (4.5,5.2) {$\|y - x\|_H = c$};

\end{tikzpicture}
\end{center}

\end{frame}

\begin{frame}
	\frametitle{Projection sur contraintes linéaires}
	
	Considérons le problème
	$$
	\min_x \frac{1}{2} \| y - x \|_H^2 \mbox{ tel que } Ax = b.
	$$
	Le problème est convexe, et satisfait la condition de Slater s'il existe au moins un point réalisable.
	
	\mbox{}
	
	Les conditions KKT s'écrivent
	\begin{align*}
	\nabla_x \frac{1}{2} \langle y-x, H(y-x) \rangle
	+ \nabla_x(Ax - b)^T \lambda &= 0 \\
	Ax - b & = 0 %\\
	%\lambda^T(Ax - b) &= 0
	\end{align*}
	ou
	\begin{align*}
	-H(y-x) + A^T \lambda &= 0 \\
	Ax & = b \\
	\end{align*}
	
\end{frame}

\begin{frame}
	\frametitle{Projection sur contraintes linéaires}
	
	Le problème peut être réorganisé comme
	\begin{align*}
	Hx + A^T \lambda &= Hy \\
	Ax + 0\lambda & = b
	\end{align*}
	donnant lieu au système linéaire
	$$
	\begin{pmatrix}
	H & A^T \\ A & 0
	\end{pmatrix}
	\begin{pmatrix}
	x \\ \lambda
	\end{pmatrix}
	=
	\begin{pmatrix}
	Hy \\ b
	\end{pmatrix}
	$$
	Si $H$ est inversible, ce qui sera le cas dans nos problèmes comme nous prendrons $H$ définie positive, nous pouvons résoudre le système en isolant $x$ et en le substituant.
	Tout d'abord, nous avons
	$$
	x = H^{-1}Hy - H^{-1}A^T \lambda = y - H^{-1}A^T \lambda
	$$
	et donc
	$$
	A\left(y - H^{-1}A^T \lambda\right) = b
	$$
	
\end{frame}

\begin{frame}
	\frametitle{Projection sur contraintes linéaires}
	
	On en tire
	$$
	AH^{-1}A^T \lambda = Ay - b
	$$
	puis, une fois $\lambda$ déterminé
	$$
	Hx = Hy - A^T \lambda
	$$
	
	%Supposons que nous pouvons obtenir deux matrices de rang plein $R$ et $N$ dont les colonnes donnent une base au domaine et à l'espace nul de $A$, respectivement.
	%$x$ peut alors être représenté comme
	%$$
	%x = Rx^R + Nx^N
	%$$
	%ce qui entraîne
	%$$
	%b = ARx^R
	%$$
	%Si $b = 0$, nous pouvons prendre $x^R = 0$, et il suit que $x = Nx^N$.
	%Suite: page 72 de CGT
	
\end{frame}

\begin{frame}
	\frametitle{Application}
	
	Considérons le problème
	\begin{align*}
	\min_x \ & f(x) \\
	\mbox{t.q. } & Ax = b
	\end{align*}
	
	\mbox{}
	
	J. B. Rosen, ``The Gradient Projection Method for Nonlinear Programming. Part I. Linear Constraints'',  Journal of the Society for Industrial and Applied Mathematics, 8(1), pp. 181-217, 1960.
	
	\mbox{}
	
	Supposons que nous avons un point de départ $x_0$ tel que $Ax_0 = b$.
	Nous souhaitons garder tous les itérés réalisables, i.e. $Ax_k = b$.
	
	\mbox{}
	
	Étant donné la direction de recherche $d_k$, nous devons dès lors avoir $Ad_k = 0$
	
\end{frame}

\begin{frame}
	\frametitle{Projection de la plus forte pente}
	
	Considérons $d_k = -\nabla f(x_k)$.
	
	\mbox{}
	
	De ce qui précède, en prenant $H = I$, et en notant $\overline{d}_k$ la projection de $d_k$,
	\begin{align*}
	\overline{d}_k &= d_k - A^T\lambda \\
	&= d_k - A^T(AA^T)^{-1}Ad_k \\
	&= \left(I-A^T(AA^T)^{-1}A\right)d_k \\
	&= -\left(I-A^T(AA^T)^{-1}A\right)\nabla f(x_k) 
	\end{align*}
	
\end{frame}

\begin{frame}
\frametitle{Projection de la plus forte pente: illustration}

\begin{center}
\begin{tikzpicture}[scale=0.85, every node/.style={transform shape},
  gradient/.style={-{Stealth[length=6pt]}, thick},
  point/.style={circle, fill=red!80!black, inner sep=2.2pt},
]

% Affine subspace (line Ax = b, direction (2,1))
\draw[very thick, blue!70!black] (-0.5,0.75) -- (7.5,4.75)
  node[pos=0.95, below right, blue!70!black, font=\small] {$Ax = b$};

% Point x_k on the line at (2,2)
\coordinate (xk) at (2,2);
\node[point] at (xk) {};
\node[below=4pt, font=\bfseries] at (xk) {$x_k$};

% -nabla f(x_k) = (1,3), endpoint at (3, 5)
\coordinate (neggradf) at (3,5);
\draw[gradient, red!70!black, very thick] (xk) -- (neggradf)
  node[above left=2pt, font=\small\bfseries, red!70!black] {$-\nabla f(x_k)$};

% Projected direction d_bar_k = (2,1), endpoint at (4,3)
\coordinate (dbar) at (4,3);
\draw[gradient, black!70!green, very thick] (xk) -- (dbar)
  node[below right=2pt, font=\small\bfseries, black!70!green] {$\overline{d}_k$};

% Parallelogram completion (dashed)
\draw[thin, dashed, gray!60] (dbar) -- (neggradf);
\draw[thin, dashed, gray!60] (xk) -- +(-1,2) -- (neggradf);

% Right angle mark at x_k
% d_bar direction: (2,1), perp direction: (-1,2)
\coordinate (ra1) at ($(xk) + (0.28,0.14)$);
\coordinate (ra2) at ($(xk) + (0.14,0.42)$);
\coordinate (racorner) at ($(ra1) + (-0.14,0.28)$);
\draw[thin, gray!70] (ra1) -- (racorner) -- (ra2);

% Label for perpendicular component
\node[gray!60, font=\scriptsize, align=center] at (0.3,3.7) {composante\\$\perp$ au noyau de $A$};

% Label for tangential component
\draw[<-, thin, gray] (4.2,3.2) -- (6.8,4.8)
  node[right, font=\small, text width=5cm, gray!70!black] {%
    $\overline{d}_k = \left(I-A^T(AA^T)^{-1}A\right)(-\nabla f(x_k))$\\[2pt]
    projection sur $\ker(A)$};

% Next iterate suggestion
\coordinate (xk1) at (5.2,3.6);
\node[circle, fill=red!80!black, inner sep=1.8pt] at (xk1) {};
\node[below=3pt, font=\small] at (xk1) {$x_{k+1}$};
\draw[-{Stealth[length=4pt]}, thick, gray!50, dashed] (xk) -- (xk1);

\end{tikzpicture}
\end{center}

\end{frame}

\begin{frame}
	\frametitle{Algorithme}
	
	\begin{itemize}
		\item 
		\textbf{Étape 0} Soit $x_0$. Poser $k = 0$.
		\item 
		\textbf{Étape 1}
		Calcul de la direction de recherche:
		$$
		\overline{d}_k = -\left(I-A^T(AA^T)^{-1}A\right)\nabla f(x_k).
		$$
		Si $\| \overline{d}_k \| = 0$, arrêt: $x_k$ est optimal.
		Sinon, aller à l'étape 2.
		\item 
		\textbf{Étape 2}
		Résoudre (approximativement)
		$$
		\min_{\alpha \geq 0} f(x_k+\alpha \overline{d}_k)
		$$
		Soit $\alpha_k$ la solution. Poser $x_{k+1} = x_k + \alpha_k \overline{d}_k$, et $k := k + 1$. Retour à l'étape 1.
	\end{itemize}
	
\end{frame}

\begin{frame}
	\frametitle{Remarques}
	
	Nous avons ici directement projeté la direction de recherche. Il est possible de montrer que $\overline{d}_k$ est solution du problème
	\begin{align*}
	\min_{d} \ & \nabla f(x_k)^Td \\
	\mbox{t.q. } & Ad = 0,\ \|d\| = 1.
	\end{align*}
	
	\mbox{}
	
	L'article de Rosen considère le cas plus général de contraintes sous la forme
	\begin{align*}
	a_i^T x = b_i,\ i = 1,\ldots,m_1 \\
	a_i^T x \leq b_i,\ i = m_1,\ldots,m \\
	\end{align*}
	L'algorithme doit alors considérer quelles sont les contraintes actives.
	
\end{frame}

\begin{frame}
\frametitle{Ensemble actif}

\begin{defn}[Ensemble actif]
L'ensemble actif $\cA(x)$ du problème d'optimisation
\begin{align*}
\min_{x \in \cX} \ & f(x) \\
\mbox{t.q. } & g_i(x) \leq 0,\ i \in \cI \\
& g_i(x) = 0,\ i \in \cE,
\end{align*}
en un point réalisable $x$ est l'ensemble des indices des contraintes d'égalité et l'ensemble des indices $i$ des contraintes d'inégalité telles que $g_i(x) = 0$, c'est-à-dire
$$
\cA(x) = \cE \cup \{ i \in \cI \,|\, g_i(x) = 0 \}
$$
\end{defn}

\end{frame}

%%% -----------------------------------------------------------------------
%%% LICQ : définition
%%% -----------------------------------------------------------------------

\begin{frame}
\frametitle{LICQ}

\begin{defn}[LICQ]
Étant donné le point $x$ et l'ensemble actif $\cA(x)$, nous disons que la qualification de contraintes d'indépendance linéaire (linear
independence constraint qualification -- LICQ) tient si l'ensemble des gradients des contraintes actives $\nabla g_i(x)$, $i \in \cA(x)$, est linéairement indépendant.
\end{defn}

\end{frame}

%%% -----------------------------------------------------------------------
%%% Lemme LICQ : énoncé complet
%%% -----------------------------------------------------------------------

\begin{frame}
\frametitle{Lemme (LICQ)}

\begin{lem}
	Soit $x^*$ un point réalisable du PNL où la LICQ tient et soit $d \in \RR^n$ tel que
	\begin{align*}
	d & \ne 0, \\
	d^T \nabla g_i(x^*) &= 0,\ i \in \cE, \\
	d^T \nabla g_i(x^*) &\leq 0,\ i \in \cA(x^*) \cap \cI.
	\end{align*}
	Alors, pour $\epsilon > 0$ assez petit, il existe un chemin $\tilde{x}(\cdot) \in C^2 \left((-\epsilon, \epsilon), \RR^n \right)$ tel que
	\begin{align*}
	\tilde{x}(0) &= x^*, \quad \frac{d}{dt} \tilde{x}(0) = d, \\
	g_i(\tilde{x}(t)) &= td^T\nabla g_i(x^*),\ i \in \cA(x^*),\ t \in (-\epsilon, \epsilon),
	\end{align*}
	de sorte que $g_i(\tilde{x}(t)) = 0$ pour $i \in \cE$, $t \in (-\epsilon, \epsilon)$, et $g_i(\tilde{x}(t)) \leq 0$ pour $i \in \cI$, $t \in [0, \epsilon)$.
\end{lem}

\end{frame}

%%% -----------------------------------------------------------------------
%%% Rappels (avant la preuve)
%%% -----------------------------------------------------------------------

\begin{frame}
\frametitle{Rappel: matrice jacobienne}

Soit
$$
F:{\begin{pmatrix}x_{1}\\\vdots \\x_{n}\end{pmatrix}}
\longmapsto {\begin{pmatrix}f_{1}(x_{1},\dots ,x_{n})\\\vdots \\f_{m}(x_{1},\dots ,x_{n})\end{pmatrix}}.
$$
La matrice jacobienne de $F$ est
$$
DF
=
\begin{pmatrix}
\nabla_x^T f_1 \\
\vdots \\
\nabla_x^T f_m
\end{pmatrix}
=
{\begin{pmatrix}{\dfrac {\partial f_{1}}{\partial x_{1}}}&\cdots &{\dfrac {\partial f_{1}}{\partial x_{n}}}\\\vdots &\ddots &\vdots \\{\dfrac {\partial f_{m}}{\partial x_{1}}}&\cdots &{\dfrac {\partial f_{m}}{\partial x_{n}}}\end{pmatrix}}.
$$
Si $n = m$, le jacobien de $F$ est le déterminant de $DF$.

\end{frame}

\begin{frame}
\frametitle{Rappel: théorème des fonctions implicites (TFI)}

Soit $F \in C^k: \Omega \subseteq \RR^n\times\RR^p \rightarrow \RR^p$, notée
$$
F(x,y) = \left( F_1(x,y), F_2(x,y), \ldots, F_p(x,y) \right)
$$
S'il existe $(a,b)$ tel que $F(a,b) = 0$ et pour lequel $D_yF(a,b)$ est inversible, alors
\begin{enumerate}
\item
il existe un voisinage $A$ de $a$ dans $\RR^n$ et un voisinage $B$ de $b$ dans $\RR^p$, une fonction $f \in C^k(A,B)$, tels que $A \times B \subseteq \Omega$ et que $\forall\, (x,y) \in A \times B$,
$$
F(x,y) = 0 \Leftrightarrow y=f(x).
$$
\item
la matrice jacobienne de $f$ par rapport à $x$ s'écrit
$$
D_x f = - \left( D_yF (x, f(x) )\right)^{-1} \left( D_xF (x, f(x))\right).
$$
\end{enumerate}

\end{frame}

%%% -----------------------------------------------------------------------
%%% Preuve du lemme LICQ
%%% -----------------------------------------------------------------------

\begin{frame}
\frametitle{Preuve du lemme -- Construction de la fonction auxiliaire}

Posons $\ell = \#\cA(x^*)$.
Puisque la LICQ tient, les $\ell$ gradients $\nabla g_i(x^*)$, $i \in \cA(x^*)$, sont linéairement indépendants. Nous pouvons donc choisir $Z \in \RR^{(n-\ell) \times n}$ telle que la matrice
$$
M = \begin{pmatrix}
\nabla g_i(x^*)^T, \ i \in \cA(x^*) \\[3pt]
Z
\end{pmatrix}
\in \RR^{n \times n}
$$
soit non singulière.

\mbox{}

Définissons $h: \RR^n \times \RR \rightarrow \RR^n$ par
$$
h(x,t) =
\begin{pmatrix}
g_i(x) - t\, d^T\nabla g_i(x^*), \ i \in \cA(x^*) \\[3pt]
Z(x-x^*-td)
\end{pmatrix}.
$$
La composante supérieure ($\ell$ lignes) mesure l'écart entre les contraintes actives et leur linéarisation le long de $d$; la composante inférieure ($n - \ell$ lignes) ancre le mouvement dans la direction $d$.

\end{frame}

\begin{frame}
\frametitle{Preuve du lemme -- Application du TFI}

La matrice jacobienne de $h$ en $(x^*, 0)$ se décompose en
$$
Dh(x^*, 0) =
\begin{pmatrix}
D_x h(x^*, 0) & D_t h(x^*, 0)
\end{pmatrix}.
$$
On obtient
$$
D_x h(x^*, 0) =
\begin{pmatrix}
\nabla g_i(x^*)^T, \ i \in \cA(x^*) \\[3pt]
Z
\end{pmatrix}
= M,
$$
et $D_t h(x^*, 0) = -Md$, de sorte que $D_x h(x^*, 0) = M$ est non singulière.

\mbox{}

Puisque $h(x^*, 0) = 0$ (car $g_i(x^*) = 0$ pour $i \in \cA(x^*)$), le TFI garantit que pour $\delta > 0$ assez petit, il existe une unique fonction $\tilde{x} \in C^2\!\left((-\delta, \delta),\, \RR^n\right)$ telle que
$$
h(x,t) = 0 \Leftrightarrow x = \tilde{x}(t), \quad t \in (-\delta, \delta).
$$

\end{frame}

\begin{frame}
\frametitle{Preuve du lemme -- Conséquences}

\textbf{Condition initiale.} De $h(\tilde{x}(0), 0) = 0$, on tire $\tilde{x}(0) = x^*$.

\mbox{}

\textbf{Contraintes actives.}
Par la composante supérieure de $h(\tilde{x}(t), t) = 0$,
$$
g_i(\tilde{x}(t)) = t\, d^T \nabla g_i(x^*), \quad i \in \cA(x^*),\ t \in (-\delta, \delta).
$$
Les hypothèses sur $d$ impliquent alors: $g_i(\tilde{x}(t)) = 0$ pour $i \in \cE$, et $g_i(\tilde{x}(t)) \leq 0$ pour $i \in \cA(x^*) \cap \cI$, $t \in [0,\delta)$.

\mbox{}

\textbf{Contraintes inactives.}
Pour $i \notin \cA(x^*)$, $g_i(x^*) < 0$. La continuité de $\tilde{x}(t)$ implique qu'il existe $\epsilon \in (0,\delta)$ tel que
$g_i(\tilde{x}(t)) < 0$ pour $i \in \cI \setminus \cA(x^*)$, $t \in (-\epsilon, \epsilon)$.

\mbox{}

\textbf{Vecteur tangent.}
Du TFI, $\frac{d}{dt} \tilde{x}(0) = -(D_x h(x^*, 0))^{-1} D_th(x^*, 0) = M^{-1}Md = d$. \qed

\end{frame}

%%% -----------------------------------------------------------------------

\begin{frame}
\frametitle{Retour au cône tangent}

Il est également possible de montrer que sous la LICQ, le cône tangent à l'ensemble réalisable $\cX$ en $x$ peut se réécrire comme
$$
T_{\cX}(x) = \{ d \,|\, d^T\nabla g_i(x) = 0,\, i \in \cE, \ d^T\nabla g_i(x) \leq 0,\, i \in \cA(x) \cap \cI \}
$$
Dès lors,
$$
\frac{d}{dt} \tilde{x}(0) \in T_{\cX}(x^*).
$$

\mbox{}

Nous allons utiliser ce lemme pour introduire la notion de chemin réalisable, lequel nous permettra de davantage caractériser les conditions prévalentes à une solution du problème d'optimisation.

\end{frame}

\begin{frame}
	\frametitle{Chemin de sortie réalisable}
	
	\begin{defn}
		Soit $x^* \in \RR^n$ un point réalisable pour le PNL et définissons $x \in C^2\left((-\epsilon,\epsilon), \RR^n \right)$ un chemin tel que
		\begin{align*}
		x(0) &= x^* \\
		d &:= \frac{d}{dt}x(0) \ne 0 \\
		g_i(x(t)) &= 0,\ i \in \cE,\ t \in (-\epsilon, \epsilon) \\
		g_i(x(t)) &\leq 0,\ i \in \cI,\ t \in [0, \epsilon)
		\end{align*}
		$x(t)$ est {\red un chemin de sortie réalisable} à partir de $x^*$ et le vecteur tangent $d = \frac{d}{dt}x(0)$ est {\red une direction de sortie réalisable} à partir de $x^*$.
	\end{defn}
	
	Nous pouvons imaginer que $x(t)$ est un morceau lisse de trajectoire d'une particule passant à travers $x^*$ au temps $t = 0$ avec une vitesse non nulle $d$ et qui se déplace dans le domaine réalisable.
	
\end{frame}

\begin{frame}
\frametitle{Chemin de sortie réalisable: illustration}

\begin{center}
\begin{tikzpicture}[scale=0.72, every node/.style={transform shape},
  constraint/.style={thick, blue!70!black},
  gradient/.style={-{Stealth[length=6pt]}, thick},
  point/.style={circle, fill=red!80!black, inner sep=2.2pt},
]

% Axes
\draw[->,gray!50] (-0.8,0) -- (7.5,0) node[right,gray] {$x_1$};
\draw[->,gray!50] (0,-0.8) -- (0,6.5) node[above,gray] {$x_2$};

% Feasible region
\fill[green!8, opacity=0.7] (0,0) -- (5,0) -- (2,3) -- (0,2) -- cycle;

% Constraint boundaries
\draw[constraint] (-0.3,5.3) -- (5.8,-0.8)
  node[pos=0.08, above right, blue!70!black, font=\small] {$g_1(x) = 0$};
\draw[constraint, blue!50!red] (-0.5,1.75) -- (4,4)
  node[pos=0.95, above left, blue!50!red, font=\small] {$g_2(x) = 0$};

\node[green!40!black, font=\small] at (1.8,1.2) {$\cX$};

% Point x*
\coordinate (xstar) at (2,3);
\node[point] at (xstar) {};
\node[left=3pt, font=\bfseries] at (xstar) {$x^*$};

% Gradients
\draw[gradient, red!70!black, dashed] (xstar) -- ++(0.9,0.9)
  node[right, font=\small, red!70!black] {$\nabla g_1(x^*)$};
\draw[gradient, orange!80!black, dashed] (xstar) -- ++(-0.6,1.2)
  node[above, font=\small, orange!80!black] {$\nabla g_2(x^*)$};

% Tangent cone (boundary rays and filled region)
% Ray 1: direction (1,-1), orthogonal to nabla g1 = (1,1). Angle = -45 deg.
% Ray 2: direction (-2,-1), orthogonal to nabla g2 = (-1,2). Angle = -153.4 deg.
\fill[black!70!green, opacity=0.08]
  (xstar) -- ++(2.2,-2.2) arc(-45:-153.4:3.11) -- cycle;
\draw[thin, black!50!green, dashed] (xstar) -- ++(2.2,-2.2);
\draw[thin, black!50!green, dashed] (xstar) -- ++(-2.78,-1.39);
\node[black!60!green, font=\scriptsize] at (1.5,0.8) {$T_{\cX}(x^*)$};

% Tangent direction d (strictly inside the cone, direction approx (1,-2))
\draw[gradient, black!80!green, very thick] (xstar) -- ++(0.75,-1.5)
  node[right=2pt, font=\small\bfseries, black!70!green] {$d = \frac{d}{dt}x(0)$};

% Feasible departure path x(t) -- stays inside cX
\draw[very thick, red!60!black,
  decoration={markings,
    mark=at position 0.25 with {\arrow{Stealth[length=5pt]}},
    mark=at position 0.55 with {\arrow{Stealth[length=5pt]}},
    mark=at position 0.82 with {\arrow{Stealth[length=5pt]}}
  }, postaction={decorate}
]
  (xstar) .. controls ++(0.9,-1.8) and ++(0.3,0.8) .. (3.2,0.6)
  node[pos=0.55, right=4pt, font=\small, red!60!black] {$x(t)$};

% Points along path
\fill[red!60!black] (2.50,2.14) circle (1.5pt);
\fill[red!60!black] (2.90,1.61) circle (1.5pt);
\fill[red!60!black] (3.28,0.98) circle (1.5pt);

% Annotations
\draw[<-, thin, gray] (3.2,0.5) -- (5.5,0.3)
  node[right, font=\small, text width=4.5cm, gray!70!black] {%
    $g_i(x(t)) \leq 0,\ i \in \cI$\\
    pour $t \in [0, \epsilon)$};

\draw[<-, thin, gray] (2.5,2.3) -- (5,4.5)
  node[right, font=\small, text width=4.5cm, gray!70!black] {%
    Chemin $C^2$, $x(0) = x^*$\\
    $\frac{d}{dt}x(0) = d$};

\end{tikzpicture}
\end{center}

\end{frame}

\end{document}